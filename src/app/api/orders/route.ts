import { NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
export async function POST(req:Request){try{const form=await req.formData();const s=await createClient();const productId=String(form.get('product_id')||'');const {data,error}=await s.rpc('create_public_order',{p_product_id:productId,p_customer_name:String(form.get('customer_name')||''),p_phone:String(form.get('phone')||''),p_address:String(form.get('address')||''),p_district:String(form.get('district')||''),p_area:String(form.get('area')||''),p_quantity:Number(form.get('quantity')||1),p_payment_method:'cod'});if(error||!data)return NextResponse.json({error:error?.message||'Order could not be created'},{status:400});return NextResponse.redirect(new URL(`/order-success/${data.order_number}`,req.url),303)}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Order failed'},{status:500})}}
